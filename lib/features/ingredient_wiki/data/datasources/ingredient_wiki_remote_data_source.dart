import 'package:flutter/material.dart';

class IngredientRemoteDataSource extends StatelessWidget {
  const IngredientRemoteDataSource({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
